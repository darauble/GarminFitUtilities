#pragma once

#include <vector>
#include <string>

class CoordinateModel {
public:
    CoordinateModel() = default;
    virtual ~CoordinateModel() = default;
    
    // TODO: Implement coordinate model
};