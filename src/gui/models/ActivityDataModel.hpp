#pragma once

#include <vector>
#include <string>

class ActivityDataModel {
public:
    ActivityDataModel() = default;
    virtual ~ActivityDataModel() = default;
    
    // TODO: Implement data model for activities
};