#pragma once

#include <vector>
#include <string>

class FileSystemModel {
public:
    FileSystemModel() = default;
    virtual ~FileSystemModel() = default;
    
    // TODO: Implement file system model
};